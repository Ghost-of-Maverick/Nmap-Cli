# 🛡️ Nmap CLI - Interface Interactiva para Nmap

Esta aplicação é uma interface de linha de comandos (CLI) para facilitar a utilização do **Nmap**, permitindo configurar alvos, tipos de scans, guardar resultados e consultar históricos de forma prática e rápida.

---

## ✅ Requisitos

- Python 3.8 ou superior
- `nmap` instalado no sistema
- Instalar dependências:

```bash
pip install -r requirements.txt
```
